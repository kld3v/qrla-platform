version: 0.2

phases:
  install:
    runtime-versions:
      php: 8.2  # Set to PHP 8.2 to match your requirements
      nodejs: 22  # Node.js version for frontend assets
    commands:
      - echo "Starting installation phase..."

      # Install Composer
      - echo "Installing Composer..."
      - curl -sS https://getcomposer.org/installer | php
      - mv composer.phar /usr/local/bin/composer
      - echo "Composer installed successfully."

  pre_build:
    commands:
      # Install PHP dependencies with Composer
      - echo "Installing PHP dependencies..."
      - composer install --no-dev --prefer-dist --optimize-autoloader

      # Install Node.js dependencies and build assets
      - echo "Installing Node.js dependencies and building assets..."
      - npm ci
      - npm run build  # or npm run build if that’s your preferred command

  build:
    commands:
      # Run database migrations (for staging, skip for production)
      - echo "Running migrations..."
      - php artisan migrate --force

      # Run PHP unit tests
      - echo "Running tests..."
      - php artisan test

  post_build:
    commands:
      # Clear Laravel cache
      - echo "Clearing cache..."
      - php artisan config:cache
      - php artisan route:cache
      - php artisan view:cache
      - echo "Cache cleared successfully."

artifacts:
  files:
    - 'app/**'
    - 'bootstrap/**'
    - 'public/**'
    - 'resources/**'
    - 'routes/**'
    - 'vendor/**'
    - 'composer.json'
    - 'composer.lock'
    - 'package.json'
    - 'package-lock.json'
  discard-paths: no

cache:
  paths:
    - '$HOME/.cache/composer/**/*'
    - 'node_modules/**/*'
